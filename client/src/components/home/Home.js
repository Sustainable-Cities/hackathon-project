import React from "react";
import GoogleApiWrapper from "../googlemap/GoogleMap";

const Home = () => {
  return (
    <div>
      <div>Home</div>
      <GoogleApiWrapper />
    </div>
  );
};

export default Home;
